# Copy to .env for local dev. .env is gitignored.
# Dev-only, NON-SECRET prefill of the Connection screen's endpoint field.
# Access/secret keys are never prefilled — type them at the gate.
VITE_SPARCD_S3_ENDPOINT=
